# Основной образ: API, бот, ingest. Тяжёлые модели живут отдельно —
# их зависимости в models/requirements.txt, torch сюда не тянется.

# --- База ------------------------------------------------------------------
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
pgvector>=0.3.0

# --- API -------------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# --- Клиенты ---------------------------------------------------------------
httpx>=0.27.0            # к сервису моделей и к LLM
aiogram>=3.13.0          # Telegram-бот
telethon>=1.36.0         # докачка истории чатов

# --- Утилиты ---------------------------------------------------------------
python-dotenv>=1.0.0
