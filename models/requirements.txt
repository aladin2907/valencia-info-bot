# Сервис моделей держит тяжёлые зависимости отдельно от основного образа:
# torch весит ~1 ГБ, и таскать его в api/bot/ingest незачем.
sentence-transformers>=3.0.0
torch>=2.2.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
